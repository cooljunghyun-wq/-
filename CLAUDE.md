# 프로젝트 컨텍스트

이 저장소는 박사논문 「한국 섬유패션산업의 지속가능한 시장가치 창출 메커니즘에 관한 연구」의 연구계획 및 문헌리서치 자동화 작업공간이다.

## 문서 구조와 우선순위

- **`docs/박사학위논문 연구계획서 초안.pdf`** — 연구 내용(주제·RQ·4개 Study·변수·가설·설계)의 **원본 기준 문서**. 다른 문서와 내용이 어긋나면 이 PDF를 따른다.
- **`docs/박사논문_주제선정_연구계획_리서치자동화_통합보고서.docx`** — 리서치 자동화 운영안(파이프라인·평가표·저널 Watchlist·키워드·요약 템플릿)의 출처 원본.
- **`docs/seed_papers.md`** — PDF 배경에서 인용된 선행연구 6편, 서지정보 미확인(문헌 검색 출발점).
- **`연구계획서.md`** (저장소 루트) — 위 두 원본을 사람이 읽기 좋게 통합한 마스터 문서. 배경/RQ/이론/4개 Study 설계, 주제선정 과정과 다중 심사관점 평가, 문헌리서치 자동화 전략(저널 워치리스트, 키워드맵, 5쪽 요약 템플릿, DB 스키마, 일일 브리핑 설계)을 담는다. 논문/연구모형 관련 작업 전에는 이 문서를 먼저 참고하되, PDF와 어긋나면 PDF가 우선.
- **`.claude/skills/lit-review/`** — 위 문서들의 기준으로 논문을 채점·요약·DB화하고, 매일 신규 논문 1편을 찾아 카카오톡(5개 필드)·Gmail(5쪽 요약)로 보내는 일일 브리핑까지 담당하는 통합 skill. **100점 평가표의 조작적 정의(점수 구간·보정식·자가검산)는 이 skill 파일에만 있다** — `연구계획서.md`는 요약만 두고 중복하지 않는다. "논문 평가해줘", "오늘의 논문 찾아줘", "이번 주 문헌 정리해줘", "브리핑 보내줘" 같은 요청에 자동 적용된다.
- **`research/`** — skill이 쓰는 데이터 저장소: `db/papers.csv`(논문 DB), `summaries/`(5쪽 요약, 커밋됨), `digests/`(주간·월간 롤업), `papers/`(제3자 논문 원문 PDF 캐시 — **커밋하지 않음**, 저작권).

연구모형·가설·질문을 변경할 때는 `연구계획서.md`를 먼저 수정하고 §13 변경이력 표에 근거를 남긴 뒤, 그 변경을 전제로 이후 작업을 진행한다. 채점 기준(점수 구간·보정식)을 바꿀 때는 skill 파일(`§2`)을 고친다.

## 일일 자동 브리핑의 현재 상태 (중요)

목표는 매일 KST 08:29에 신규 논문 1편을 찾아 5개 필드(제목/저널/연도/인용수/초록)를 카카오톡으로, 85점 이상이면 5쪽 요약을 Gmail로 보내는 것(`연구계획서.md` §12.11, skill §5~§8).

- **카카오톡**: `mcp__PlayMCP__KakaotalkChat-MemoChat` 도구가 이 계정에 연결되어 있어 바로 작동한다.
- **Gmail**: 발송 가능한 MCP 커넥터가 아직 연결되어 있지 않다. 연결 전까지 skill은 이메일 본문을 만들어 보여주는 데서 멈추고, "보냈다"고 보고하지 않는다. Gmail 발송이 필요하면 claude.ai 커넥터 설정에서 Gmail(또는 동등 이메일) 커넥터를 연결해야 한다.
- **매일 08:29 자동 실행 자체**: `SKILL.md`는 절차서일 뿐 스스로 깨어나지 않는다. 실제로 매일 자동 실행하려면 Claude Code Remote **Routine**(cron `29 23 * * *`, UTC)을 등록해야 하며, 이는 매일 실제 메시지를 발송하는 지속 동작이므로 사용자 확인 없이 스킬이 스스로 등록하지 않는다.
- **논문 발견**: Google Scholar는 공식 API가 없고 자동조회를 차단하므로 직접 크롤링하지 않는다. 1순위는 Gmail의 Scholar Alert 메일 파싱(Gmail 연동 필요), 2순위는 OpenAlex API(무료·무키, Scholar와 동등한 인용 데이터 제공), 최후 수단은 WebSearch.

## 개발 규칙 (향후 코드/파이프라인 구현 시)

- 민감정보는 `.env`에만 둔다(API 키, Gmail·Kakao 토큰 등). 코드에서는 환경변수로 읽는다. 저장소 루트 `.gitignore` 참고.
- `docs/`, `research/summaries/`, `research/digests/`의 사람이 읽는 문서는 한글 파일명 유지. `.claude/skills/`와 향후 `src/`의 코드·스킬 이름은 영문 snake_case(공백·한글 금지).
- Python으로 파이프라인을 구현할 경우: 3.10+, 주석은 한국어, 함수/변수명은 영어 snake_case, 각 수집·보강 소스는 독립 동작(하나 실패해도 기록 후 나머지 계속 진행), 로그는 콘솔에 실시간 출력.
- 원문에 없는 결과·수치·서지정보를 생성하지 않는다. 되지 않는 것(예: 커넥터 미연결)을 됐다고 보고하지 않는다.
