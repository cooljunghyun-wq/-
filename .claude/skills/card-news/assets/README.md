# assets/

Drop the real KOFOTI logo file here (e.g. `kofoti_logo.png`) when one is
available, then point `brand.logo_path` in `cards.json` at it. Until then,
leave this folder empty — `scripts/render_cards.py` falls back to a text
badge with the org's name automatically (see `../references/brand.md`).
